<h1>Tipo de Novedades</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @new_types.each do |new_type| %>
      <tr>
        <td><%= new_type.name %></td>
        <td><%= new_type.description %></td>
        <td><%= link_to 'Ver', new_type %></td>
        <td><%= link_to 'Editar', edit_new_type_path(new_type) %></td>
        <td><%= link_to 'Eliminar', new_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Tipo Novedad', new_new_type_path %>