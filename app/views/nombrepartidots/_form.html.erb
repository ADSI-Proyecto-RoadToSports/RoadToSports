<%= form_for(@nombrepartidot) do |f| %>
  <% if @nombrepartidot.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartidot.errors.count, "error") %> prohibited this nombrepartidot from being saved:</h2>

      <ul>
      <% @nombrepartidot.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit 'Guardar' %>
  </div>
<% end %>
