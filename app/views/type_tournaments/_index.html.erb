<h1>Tipo Torneos</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
    </tr>
  </thead>

  <tbody>
    <% @type_tournaments.each do |type_tournament| %>
      <tr>
        <td><%= type_tournament.name %></td>
        <td><%= type_tournament.description %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
