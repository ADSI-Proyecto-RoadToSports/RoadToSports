<%= form_for(@type_tournament) do |f| %>
  <% if @type_tournament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_tournament.errors.count, "error") %> prohibited this type_tournament from being saved:</h2>

      <ul>
      <% @type_tournament.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit 'Crear Torneo' %>
  </div>
<% end %>
