<h1>Tipo Torneos</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @type_tournaments.each do |type_tournament| %>
      <tr>
        <td><%= type_tournament.name %></td>
        <td><%= type_tournament.description %></td>
        <td><%= link_to 'Ver', type_tournament %></td>
        <td><%= link_to 'Editar', edit_type_tournament_path(type_tournament) %></td>
        <td><%= link_to 'Eliminar', type_tournament, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'Nuevo Tipo de Torneo', new_type_tournament_path, :method=>"get", class: "btn btn-info" %>