<div class="contenidop">
  <div class="img1"></div>
  <div class="img2"></div>
    <ul class="nav nav-tabs" style="margin-bottom: 15px;">

    </ul>
  <div id="myTabContent" class="tab-content">
    <div class="" id="inicio">
      <h1>Reglas</h1>
      <%= form_tag rules_path, :method => 'get' do %>
        <% content_tag :label do %>
          <%= text_field_tag :search, params[:search], :placeholder => "Buscar por nombre..." %>
          <%= submit_tag "Buscar", :name => nil %>
        <% end %>
      <% end %>
      <div class="paginacion">Reglas: <b><%= @rules.count %> </b><%= will_paginate @rules, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente &raquo;' %></div>
      <table class="table table-striped table-hover ">
        <thead>
          <tr>
            <th>Nombre</th>
            <th>Descripción</th>
            <th>Deporte</th>
            <th>Tipo de regla</th>
            <th>Estado</th>
            <th colspan="3">Opciones</th>
          </tr>
        </thead>
        <tbody>
          <%= render @rules%>
        </tbody>
      </table>
      <br>
      <%= if can? :update, @rule
        button_to 'Nueva Regla', new_rule_path, remote: true, :method=>"get", class: "btn btn-info"
      end %>
      <br>
      <div class="paginacion">Reglas: <b><%= @rules.count %> </b><%= will_paginate @rules, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente &raquo;' %></div>
    </div>
  </div>
</div>