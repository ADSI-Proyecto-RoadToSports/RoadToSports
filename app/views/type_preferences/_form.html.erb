<%= form_for(@type_preference) do |f| %>
  <% if @type_preference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_preference.errors.count, "error") %> prohibited this type_preference from being saved:</h2>

      <ul>
      <% @type_preference.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
