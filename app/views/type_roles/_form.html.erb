<%= form_for(@type_role) do |f| %>
  <% if @type_role.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_role.errors.count, "error") %> prohibited this type_role from being saved:</h2>

      <ul>
      <% @type_role.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
