<%= form_for(@nombrepartidom) do |f| %>
  <% if @nombrepartidom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartidom.errors.count, "error") %> prohibited this nombrepartidom from being saved:</h2>

      <ul>
      <% @nombrepartidom.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
