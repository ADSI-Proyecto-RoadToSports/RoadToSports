<%= form_for(@type_information) do |f| %>
  <% if @type_information.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_information.errors.count, "error") %> prohibited this type_information from being saved:</h2>

      <ul>
      <% @type_information.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
