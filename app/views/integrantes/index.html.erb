<div class="contenido ">
<div class="img1"></div>
<div class="img2"></div>
<ul class="nav nav-tabs" style="margin-bottom: 15px;">

</ul>
<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade active in cont1" id="inicio">
<h1>Listing integrantes</h1>

<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Document type</th>
      <th>Documento</th>
      <th>Ficha</th>
      <th>Team</th>
      <th>Limite</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <% @integrantes.each do |integrante| %>
      <tr>
        <td><%= integrante.nombre %></td>
        <td><%= integrante.document_type.name %></td>
        <td><%= integrante.documento %></td>
        <td><%= integrante.ficha %></td>
        <td><%= integrante.team.nombre %></td>
        <td><%= integrante.limite %></td>
        <td><%= link_to 'Show', team_integrante_path(@team,integrante) %></td>
        <td><%= link_to 'Edit', edit_team_integrante_path(@team,integrante) %></td>
        <td><%= link_to 'Destroy', team_integrante_path(@team,integrante), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
     <%# <%=
        if integrante.team.sport_id == 1
          
          if @integrantes.count >= 2
            integrante.limite == true
          end 

        else
          "no"
        end
        %>
    <% end %>
  </tbody>
</table>

<br>
<%= link_to 'New Integrante', new_team_integrante_path(@team) %>
<%#<%=
  if integrante.limite == true
    link_to 'New Integrante', new_team_integrante_path(@team) 
  end

%>


</div></div></div>