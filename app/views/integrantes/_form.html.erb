<%= form_for [@team,@integrante] do |f| %>
<%#<% if current_user.email != @team.lider and current_user.admin == false %>
 <%# <div class="alerta">
  <div class="alertah">
    <h2 class="panel-title">Acceso No autorizado.</h2>
  </div>
  <div class="panel-body">
    <h1> </h1>
    <p>El usuario: <b><%= current_user.nombre %><%#</b> No esta autorizado para acceder a esta página.</p>
 <%# </div>
</div>

<% else %>
  <% if @integrante.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@integrante.errors.count, "error") %> prohibited this integrante from being saved:</h2>

      <ul>
      <% @integrante.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <label>Tipo de documento</label><br>
    <%= f.collection_select :documenttype_id, DocumentType.all, :id, :name, class: "form-control", prompt: "Seleccione Un tipo de documento" %>
  </div>
  <div class="field">
    <%= f.label :documento %><br>
    <%= f.text_field :documento %>
  </div>
  <div class="field">
    <%= f.label :ficha %><br>
    <%= f.text_field :ficha %>
  </div>
  <div class="field">
    <%= f.label :puede_jugar? %><br>
    <%= f.check_box :lesion %>
  </div>
  <div class="actions">
    <%= f.submit  "Guardar"%>
  </div>
<%# end %>
<% end %>