<h1>Tipo Documentos</h1>

  <table  class="table table-striped table-hover ">
    <thead>
      <tr>
        <th>Nombre</th>
        <th>Descripción</th>
        <th colspan="3">Opciones</th>
      </tr>
    </thead>

    <tbody>
      <% @document_types.each do |document_type| %>
        <tr>
          <td><%= document_type.name %></td>
          <td><%= document_type.description %></td>
          <td><%= link_to 'Ver', document_type %></td>
          <td><%= link_to 'Editar', edit_document_type_path(document_type) %></td>
          <td><%= link_to 'Eliminar', document_type, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>

  <br>

  <%= button_to 'Nuevo Tipo de Documento', new_document_type_path, :method=>"get", class: "btn btn-info" %>
