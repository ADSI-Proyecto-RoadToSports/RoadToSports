<div class="contenidop ">
  <div class="img1"></div>
  <div class="img2"></div>
    <ul class="nav nav-tabs" style="margin-bottom: 15px;">
    </ul>
  <div id="myTabContent" class="tab-content">
    <div class="" id="inicio">
    <div class="slideExpandUp">
    <%= render 'nombrepartidods/index' %>
    </div>
<h1>Encuentros de ajedrez</h1>
 <div class="paginacion">Encuentros: <b><%= @pajedrezs.count %> </b><%= will_paginate @pajedrezs, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente &raquo;' %></div>
<table class="table table-striped table-hover ">
  <thead>
    <tr>
      <th colspan="2">Participante1</th>
      <th>Puntos_Torneo</th>
      <th colspan="2">Participante2</th>
      <th>Puntos_Torneo</th>
      <th>Responsable</th>
      <th colspan="3">Opciones</th>
    </tr>
  </thead>

  <tbody>
    <%= render @pajedrezs %>
  </tbody>
</table>

<br>

<%= if can? :create, @pajedrez
 button_to 'Nuevo encuentro', new_pajedrez_path, remote: true , :method => "get",class: "btn btn-info"
 end %>
 <div class="paginacion">Encuentros: <b><%= @pajedrezs.count %> </b><%= will_paginate @pajedrezs, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente &raquo;' %></div>
</div>
<p><%= link_to 'Generar(PDF)', pajedrezs_path(@pajedrezs, format: "pdf")%></p>
</div>
</div>