<h1>Listing modalities</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Players</th>
      <th>Activity</th>
      <th>Tournament</th>
      <th>Modalities type</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @modalities.each do |modality| %>
      <tr>
        <td><%= modality.name %></td>
        <td><%= modality.players %></td>
        <td><%= modality.activity %></td>
        <td><%= modality.tournament %></td>
        <td><%= modality.modalities_type %></td>
        <td><%= link_to 'Show', modality %></td>
        <td><%= link_to 'Edit', edit_modality_path(modality) %></td>
        <td><%= link_to 'Destroy', modality, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Modalitie', new_modality_path %>
