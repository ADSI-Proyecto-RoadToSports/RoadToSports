<h1>Tipos de Roles</h1>

<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @type_rules.each do |type_rule| %>
      <tr>
        <td><%= type_rule.name %></td>
        <td><%= type_rule.description %></td>
        <td><%= link_to 'Ver', type_rule %></td>
        <td><%= link_to 'Editar', edit_type_rule_path(type_rule) %></td>
        <td><%= link_to 'Eliminar', type_rule, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Tipo de Rol', new_type_rule_path %>
