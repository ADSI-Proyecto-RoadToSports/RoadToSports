<div class="contenido ">
<div class="img1"></div>
<div class="img2"></div>
<ul class="nav nav-tabs" style="margin-bottom: 15px;">

</ul>
<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade active in cont1" id="inicio">
<h1>Lista Deportes</h1>
<%= form_tag sports_path, :method => 'get' do %> 

   <% content_tag :label do %> 

   Search term:
   <%= text_field_tag :search, params[:search] %> 

   <%= submit_tag "Buscar", :name => nil %> 

   <% end %> 

  <% end %>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripción</th>
      <th>Modalidad</th>
      <th>Regla</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sports.each do |sport| %>
      <tr>
        <td><%= sport.name %></td>
        <td><%= sport.description %></td>
        <td><%= sport.modalitie %></td>
        <td><%= sport.rule %></td>
        <td><%= link_to 'Ver', sport %></td>
        <td><%= link_to 'Editar', edit_sport_path(sport) %></td>
        <td><%= link_to 'Eliminary', sport, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= button_to 'Nuevo Deporte', new_sport_path, :method=>"get" %><br>
<%= will_paginate @sports, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente 
&raquo;' %> 
<br> 
Deportes: <%= @sports.count %>
</div></div></div>