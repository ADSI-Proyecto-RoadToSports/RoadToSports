<div class="contenido ">
<div class="img1"></div>
<div class="img2"></div>
<ul class="nav nav-tabs" style="margin-bottom: 15px;">

</ul>
<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade active in cont1" id="inicio">
<h1>Torneos</h1>
<%= form_tag tournaments_path, :method => 'get' do %>
<% content_tag :label do %>
<%= text_field_tag :search, params[:search] %>
<%= submit_tag "Buscar", :name => nil %>
<% end %>
<% end %>
<table>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Tipo de Modalidad</th>
      <th>Estado</th>
      <th>Fecha</th>
      <th>Tipo Torneo</th>
      <th>Aceptación</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tournaments.each do |tournament| %>
      <tr>
        <td><%= tournament.name %></td>
        <td><%= tournament.modalitiestype %></td>
        <td><%= tournament.state %></td>
        <td><%= tournament.date %></td>
        <td><%= tournament.typetournament %></td>
        <td><%= tournament.acceptance %></td>
        <td><%= link_to 'Ver', tournament %></td>
        <td><%= link_to 'Editar', edit_tournament_path(tournament) %></td>
        <td><%= link_to 'Eliminar', tournament, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Nuevo Torneo', new_tournament_path %><br>
<%= will_paginate @tournaments, :previous_label => '&laquo; Anterior', :next_label => 'Siguiente 
&raquo;' %> 
<br> 
Torneos: <%= @tournaments.count %>
</div>
</div>
</div>