<%= form_for(@type_change) do |f| %>
  <% if @type_change.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@type_change.errors.count, "error") %> prohibited this type_change from being saved:</h2>

      <ul>
      <% @type_change.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
