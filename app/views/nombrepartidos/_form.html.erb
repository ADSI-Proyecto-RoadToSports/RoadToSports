<%= form_for(@nombrepartido) do |f| %>
  <% if @nombrepartido.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartido.errors.count, "error") %> prohibited this nombrepartido from being saved:</h2>

      <ul>
      <% @nombrepartido.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
