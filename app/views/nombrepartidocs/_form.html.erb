<%= form_for(@nombrepartidoc) do |f| %>
  <% if @nombrepartidoc.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartidoc.errors.count, "error") %> prohibited this nombrepartidoc from being saved:</h2>

      <ul>
      <% @nombrepartidoc.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
