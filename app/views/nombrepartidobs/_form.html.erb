<%= form_for(@nombrepartidob) do |f| %>
  <% if @nombrepartidob.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartidob.errors.count, "error") %> prohibited this nombrepartidob from being saved:</h2>

      <ul>
      <% @nombrepartidob.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
