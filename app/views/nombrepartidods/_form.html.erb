<%= form_for(@nombrepartidod) do |f| %>
  <% if @nombrepartidod.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@nombrepartidod.errors.count, "error") %> prohibited this nombrepartidod from being saved:</h2>

      <ul>
      <% @nombrepartidod.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit "Guardar" %>
  </div>
<% end %>
